name: 'Build and publish Docker'
author: 'Metroscope'
description: 'Uses the git tag as the docker tag and pushes the container'
inputs:
  TAG:
    description: 'tag wanted'
    required: true
  services:
    description: 'list of services to be processed'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
